// Fill out your copyright notice in the Description page of Project Settings.

#include "ILabel.h"
#include "Components/WidgetComponent.h"

void IILabel::RotateTextWidget(UWidgetComponent TextWidgetComponent)
{
	if(TextWidgetComponent.GetUserWidgetObject() != nullptr)
	{
		
	}
}

/// To use on Interactable OnNotifyEnter
void IILabel::DisplayTextWidget(UWidgetComponent TextWidgetComponent)
{
	if(TextWidgetComponent.GetUserWidgetObject() != nullptr)
	{
		
	}
}

/// To use on Interactable OnNotifyExit
void IILabel::HideTextWidget(UWidgetComponent TextWidgetComponent)
{
	if(TextWidgetComponent.GetUserWidgetObject() != nullptr)
	{
		
	}
}

